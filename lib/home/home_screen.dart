import 'package:flutter/material.dart';

class HomePageSc extends StatelessWidget {
  const HomePageSc({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
